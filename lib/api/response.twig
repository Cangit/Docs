    
    <h1>response()</h1>
    <p class="parameters">
        <b>$content</b> <em>string</em> = ''<br />
        <b>$status</b> <em>int</em> = 200<br />
        <b>$headers</b> <em>arr</em> = []<br />
    </p>
    <p class="response">
        Returns a symfony response object
    </p>

    <p>Constructs a symfony response object based on content, status and headers. (utf8 headers are generated by default.)</p>

    <h2>Usage</h2>
    <pre>
&lt;?php

$html = $twig-&gt;render('examples/welcome.twig');
$response = $app->response($html);

return $response;

    </pre>